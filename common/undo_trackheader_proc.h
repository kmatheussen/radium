#pragma once

extern LANGSPEC void ADD_UNDO_FUNC(TrackHeader(
                                               NInt blocknum,
                                               NInt tracknum
                                               ));

